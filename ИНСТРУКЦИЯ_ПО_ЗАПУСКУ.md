# 📖 Инструкция по запуску проекта Social Wishlist

## 🔧 Требования

Перед запуском убедитесь, что у вас установлено:

- **Node.js** версии 18 или выше ([скачать](https://nodejs.org/))
- **Python** версии 3.11 или выше ([скачать](https://www.python.org/))
- **PostgreSQL** версии 14 или выше ([скачать](https://www.postgresql.org/download/))
  - Или используйте облачную базу данных (Supabase, Railway, Render)

---

## 🗄️ Шаг 1: Настройка базы данных PostgreSQL

### ⭐ Вариант A: Облачная база данных (Supabase - РЕКОМЕНДУЕТСЯ для Windows)

**Это самый простой способ, не требует установки PostgreSQL!**

1. **Зарегистрируйтесь на Supabase:**
   - Перейдите на [https://supabase.com](https://supabase.com)
   - Нажмите "Start your project" и зарегистрируйтесь (можно через GitHub)

2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите организацию (или создайте новую)
   - Введите название проекта (например, "wishlist-app") 
   - Введите пароль для базы данных (запомните его!) 6NKmhUIXpDIFqVMj
   - Выберите регион (ближайший к вам)
   - Нажмите "Create new project"
   - Подождите 1-2 минуты, пока проект создается

3. **Получите Connection String:**
   - После создания проекта перейдите в **Settings** → **Database**
   - Найдите раздел **Connection string**
   - Выберите вкладку **URI**
   - Скопируйте строку подключения (она выглядит так: `postgresql://postgres.xxxxx:[YOUR-PASSWORD]@aws-0-eu-central-1.pooler.supabase.com:6543/postgres`)
   - **ВАЖНО:** Замените `[YOUR-PASSWORD]` на ваш пароль, который вы указали при создании проекта
   - Замените `postgresql://` на `postgresql+asyncpg://` для использования в проекте

   **Пример итоговой строки:**
   ```
   postgresql+asyncpg://postgres.xxxxx:мой_пароль123@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
   ```

4. **Готово!** Используйте эту строку в файле `.env` backend (см. Шаг 2)

### Вариант B: Локальная установка PostgreSQL (для продвинутых пользователей)

**Если вы хотите использовать локальную базу данных:**

1. **Скачайте и установите PostgreSQL:**
   - Перейдите на [https://www.postgresql.org/download/windows/](https://www.postgresql.org/download/windows/)
   - Скачайте установщик (рекомендуется использовать установщик от EnterpriseDB)
   - Запустите установщик и следуйте инструкциям
   - **ВАЖНО:** При установке запомните пароль для пользователя `postgres`
   - Убедитесь, что опция "Add PostgreSQL to PATH" отмечена

2. **После установки перезапустите терминал** (или Git Bash)

3. **Создайте базу данных:**
   ```bash
   # Войдите в PostgreSQL (введите пароль, который указали при установке)
   psql -U postgres
   
   # В консоли PostgreSQL выполните:
   CREATE DATABASE wishlist_db;
   
   # Выйдите из psql
   \q
   ```

4. **Используйте Connection String:**
   ```
   postgresql+asyncpg://postgres:ваш_пароль@localhost:5432/wishlist_db
   ```

### Вариант C: Использование pgAdmin (графический интерфейс)

1. Зарегистрируйтесь на [Supabase](https://supabase.com)
2. Создайте новый проект
3. Перейдите в Settings → Database
4. Скопируйте Connection String (URI)
5. Формат будет: `postgresql://postgres:[PASSWORD]@[HOST]:5432/postgres`
6. Замените `postgresql://` на `postgresql+asyncpg://` для использования в проекте

---

## 🔙 Шаг 2: Настройка Backend (FastAPI)

1. **Откройте терминал и перейдите в папку backend:**
   ```bash
   cd backend
   ```

2. **Создайте виртуальное окружение Python:**
   
   **Windows:**
   ```bash
   python -m venv venv
   venv\Scripts\activate
   ```
   
   **macOS/Linux:**
   ```bash
   python3 -m venv venv
   source venv/bin/activate
   ```

3. **Установите зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

4. **Создайте файл `.env`:**
   
   Скопируйте пример:
   ```bash
   # Windows
   copy .env.example .env
   
   # macOS/Linux
   cp .env.example .env
   ```

5. **Откройте файл `.env` и настройте переменные окружения:**
   ```env
   # URL базы данных (замените на свои данные)
   DATABASE_URL=postgresql+asyncpg://пользователь:пароль@localhost:5432/wishlist_db
   
   # Секретный ключ для JWT (сгенерируйте случайную строку)
   SECRET_KEY=ваш-секретный-ключ-минимум-32-символа-изменяйте-в-продакшене
   
   # Разрешенные источники для CORS (URL фронтенда)
   ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
   
   # Время жизни токена (в минутах)
   ACCESS_TOKEN_EXPIRE_MINUTES=10080
   ```

   **Пример для Supabase:**
   ```env
   DATABASE_URL=postgresql+asyncpg://postgres.xxxxx:ваш_пароль@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
   SECRET_KEY=my-super-secret-key-change-in-production-12345678901234567890
   ALLOWED_ORIGINS=http://localhost:5173
   ```
   
   **Пример для локальной базы данных:**
   ```env
   DATABASE_URL=postgresql+asyncpg://postgres:ваш_пароль@localhost:5432/wishlist_db
   SECRET_KEY=my-super-secret-key-change-in-production-12345678901234567890
   ALLOWED_ORIGINS=http://localhost:5173
   ```

6. **Запустите backend сервер:**
   ```bash
   uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
   ```

   Сервер запустится на `http://localhost:8000`
   
   📚 API документация будет доступна на `http://localhost:8000/docs`

---

## 🎨 Шаг 3: Настройка Frontend (React)

1. **Откройте новый терминал и перейдите в папку frontend:**
   ```bash
   cd frontend
   ```

2. **Установите зависимости:**
   ```bash
   npm install
   ```

3. **Создайте файл `.env`:**
   
   ```bash
   # Windows
   copy .env.example .env
   
   # macOS/Linux
   cp .env.example .env
   ```

4. **Откройте файл `.env` и настройте URL бэкенда:**
   ```env
   # URL API бэкенда
   VITE_API_URL=http://localhost:8000
   
   # URL WebSocket для реального времени
   VITE_WS_URL=ws://localhost:8000
   ```

5. **Запустите frontend сервер разработки:**
   ```bash
   npm run dev
   ```

   Приложение откроется на `http://localhost:5173`

---

## ✅ Шаг 4: Проверка работы

1. **Откройте браузер** и перейдите на `http://localhost:5173`

2. **Зарегистрируйте нового пользователя:**
   - Нажмите "Sign Up" или "Get Started"
   - Заполните форму (Email, Имя, Пароль)
   - Нажмите "Sign Up"

3. **Создайте первый wishlist:**
   - После регистрации вы попадете в Dashboard
   - Нажмите "Create Wishlist"
   - Введите название и описание
   - Нажмите "Create"

4. **Добавьте первый подарок:**
   - Нажмите "Add Item"
   - Введите название, цену
   - (Опционально) Вставьте URL товара и нажмите "Auto-fill"
   - Нажмите "Add Item"

5. **Поделитесь wishlist:**
   - Нажмите кнопку "Share"
   - Скопируйте ссылку
   - Откройте ссылку в режиме инкогнито (чтобы проверить публичный вид)

---

## 🐛 Решение проблем

### Ошибка подключения к базе данных

**Проблема:** `asyncpg.exceptions.InvalidPasswordError` или `Connection refused`

**Решение:**
- Проверьте правильность `DATABASE_URL` в `.env`
- Убедитесь, что PostgreSQL запущен
- Проверьте логин и пароль
- Для Supabase: используйте Connection Pooling URL

### Ошибка CORS

**Проблема:** `CORS policy: No 'Access-Control-Allow-Origin' header`

**Решение:**
- Убедитесь, что `ALLOWED_ORIGINS` в backend `.env` содержит URL фронтенда
- Перезапустите backend сервер

### Ошибка при установке зависимостей Python

**Проблема 1:** `Microsoft Visual C++ 14.0 is required`

**Решение (Windows):**
- Установите [Microsoft C++ Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/)

**Проблема 2:** `pydantic-core` требует Rust компилятор

**Решение:**
- **Вариант A (рекомендуется):** Обновите pip и используйте более новую версию pydantic:
  ```bash
  python -m pip install --upgrade pip
  pip install pydantic>=2.9.0
  pip install -r requirements.txt
  ```
  
- **Вариант B:** Установите Rust (если вариант A не помог):
  - Скачайте и установите [Rust](https://rustup.rs/)
  - Перезапустите терминал
  - Попробуйте установить зависимости снова

**Проблема 3:** Файл `.env.example` не найден

**Решение:**
- Создайте файл `.env` вручную в папке `backend/` со следующим содержимым:
  ```env
  DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/wishlist_db
  SECRET_KEY=your-secret-key-here-change-in-production
  ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
  ```

### Ошибка при установке зависимостей Node.js

**Проблема:** Различные ошибки npm

**Решение:**
```bash
# Очистите кэш
npm cache clean --force

# Удалите node_modules и установите заново
rm -rf node_modules package-lock.json
npm install
```

### Backend не создает таблицы

**Проблема:** Таблицы не создаются автоматически

**Решение:**
- Приложение создает таблицы при первом запуске
- Если не работает, запустите миграции вручную:
  ```bash
  cd backend
  alembic upgrade head
  ```

---

## 🚀 Запуск в режиме продакшена

### Backend (Render/Railway)

1. Загрузите код на GitHub
2. Создайте новый Web Service на Render/Railway
3. Подключите репозиторий
4. Установите переменные окружения:
   - `DATABASE_URL`
   - `SECRET_KEY`
   - `ALLOWED_ORIGINS` (URL вашего фронтенда)
5. Команда запуска: `uvicorn app.main:app --host 0.0.0.0 --port $PORT`

### Frontend (Vercel)

1. Загрузите код на GitHub
2. Импортируйте проект в Vercel
3. Установите переменные окружения:
   - `VITE_API_URL` (URL вашего бэкенда)
   - `VITE_WS_URL` (WebSocket URL, используйте `wss://` для HTTPS)
4. Deploy

---

## 📝 Полезные команды

### Backend
```bash
# Запуск сервера
uvicorn app.main:app --reload

# Создание миграции
alembic revision --autogenerate -m "описание изменений"

# Применение миграций
alembic upgrade head

# Откат миграции
alembic downgrade -1
```

### Frontend
```bash
# Запуск dev сервера
npm run dev

# Сборка для продакшена
npm run build

# Просмотр сборки
npm run preview
```

---

## 🎉 Готово!

Теперь ваш проект должен работать. Если возникнут проблемы, проверьте:

1. ✅ PostgreSQL запущен и доступен
2. ✅ Backend запущен на порту 8000
3. ✅ Frontend запущен на порту 5173
4. ✅ Переменные окружения настроены правильно
5. ✅ CORS настроен правильно

Удачи! 🚀

