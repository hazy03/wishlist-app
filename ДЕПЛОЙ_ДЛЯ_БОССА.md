# 🚀 Быстрый деплой для демонстрации

## ⚡ За 10 минут

### 1. Frontend → Vercel (3 мин)
1. Зайди на [vercel.com](https://vercel.com) → войди через GitHub
2. "New Project" → выбери репозиторий
3. Настройки:
   - **Root Directory:** `frontend`
   - **Framework:** Vite
   - **Build:** `npm run build`
   - **Output:** `dist`
4. Нажми "Deploy"
5. ✅ Скопируй URL (например: `your-app.vercel.app`)

---

### 2. База данных → Supabase (2 мин)
1. Зайди на [supabase.com](https://supabase.com) → создай проект (бесплатно)
2. Settings → Database → Connection Pooling
3. Скопируй **Connection Pooling URL** (порт 6543)
4. Формат: `postgresql+asyncpg://postgres:[PASSWORD]@[HOST]:6543/postgres`

---

### 3. Backend → Render (5 мин)
1. Зайди на [render.com](https://render.com) → войди через GitHub
2. "New +" → "Web Service" → выбери репозиторий
3. Настройки:
   - **Name:** `wishlist-api`
   - **Root Directory:** `backend`
   - **Runtime:** Python 3
   - **Build:** `pip install -r requirements.txt`
   - **Start:** `uvicorn app.main:app --host 0.0.0.0 --port $PORT`
4. **Environment Variables:**
   ```
   DATABASE_URL=твой-supabase-url-из-шага-2
   SECRET_KEY=любой-длинный-случайный-ключ-минимум-32-символа
   ALLOWED_ORIGINS=https://твой-frontend.vercel.app
   FRONTEND_URL=https://твой-frontend.vercel.app
   ```
5. Нажми "Create Web Service"
6. ✅ Скопируй URL (например: `your-api.onrender.com`)

---

### 4. Обновить переменные (2 мин)
1. **В Render:** обнови `ALLOWED_ORIGINS` и `FRONTEND_URL` на URL из Vercel
2. **В Vercel:** добавь переменные:
   ```
   VITE_API_URL=https://твой-backend.onrender.com
   VITE_WS_URL=wss://твой-backend.onrender.com
   ```
3. Перезапусти оба сервиса (в Render: "Manual Deploy")

---

## ✅ Готово!

**Твои ссылки:**
- 🌐 Frontend: `https://твой-app.vercel.app`
- 🔧 API: `https://твой-api.onrender.com/docs`

**Покажи боссу:**
1. Главную страницу
2. Регистрацию → создание вишлиста
3. Публичную ссылку (кнопка "Поделиться")
4. Реалтайм обновления (открой в двух окнах)

---

## 🆘 Если не работает

1. **Проверь логи в Render** (вкладка "Logs")
2. **Проверь переменные окружения** (все ли заполнены)
3. **Проверь CORS** (URL в `ALLOWED_ORIGINS` должен точно совпадать)
4. **Проверь базу** (Supabase проект должен быть активен)

---

## 📝 Чеклист

- [ ] Frontend на Vercel работает
- [ ] Backend на Render работает
- [ ] База данных Supabase подключена
- [ ] Можно зарегистрироваться
- [ ] Можно создать вишлист
- [ ] Публичная ссылка открывается

**Время: ~10 минут** ⏱️

