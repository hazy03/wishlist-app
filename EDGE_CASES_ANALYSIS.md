# Анализ Edge Cases в Social Wishlist

## ✅ Edge Cases, которые уже обрабатываются штатно

### 1. **Конкурентные операции (Concurrency)**
- ✅ **Одновременное резервирование одного товара**: Используется `with_for_update()` для блокировки записи
- ✅ **Одновременные взносы в групповой подарок**: Блокировка + проверка суммы перед добавлением
- ✅ **Проверка превышения суммы взноса**: Валидация `amount > remaining` с понятным сообщением

### 2. **Аутентификация и авторизация**
- ✅ **Несуществующий email при входе**: Конкретное сообщение "User with this email does not exist"
- ✅ **Неправильный пароль**: Конкретное сообщение "Incorrect password"
- ✅ **OAuth пользователь пытается войти по паролю**: Сообщение о необходимости использовать OAuth
- ✅ **Истекший токен**: Автоматический редирект на `/login` при 401
- ✅ **Гость без имени при резервировании**: Требование `guest_name` для неавторизованных

### 3. **Валидация данных**
- ✅ **Отрицательная сумма взноса**: Проверка `amount <= 0`
- ✅ **Превышение суммы взноса**: Проверка `amount > remaining` с точным сообщением
- ✅ **Резервирование группового подарка**: Проверка `is_group_gift` с ошибкой
- ✅ **Взнос в негрупповой подарок**: Проверка `!is_group_gift` с ошибкой
- ✅ **Дублирование email при регистрации**: Проверка существующего пользователя

### 4. **Обработка URL и автозаполнение**
- ✅ **Таймаут запроса**: Обработка `httpx.TimeoutException` с кодом 408
- ✅ **Ошибка сети**: Обработка `httpx.RequestError` с понятным сообщением
- ✅ **Невалидный URL**: Обработка общих исключений с кодом 422
- ✅ **Относительные URL изображений**: Преобразование в абсолютные URL
- ✅ **Несколько методов поиска цены**: 4 метода (OG, JSON-LD, meta, regex)

### 5. **База данных**
- ✅ **Ошибка подключения при старте**: Graceful degradation с информативными сообщениями
- ✅ **Автоматическая миграция колонок**: Проверка существования перед добавлением
- ✅ **Cascade удаление**: Правильная настройка `ondelete` в моделях
- ✅ **Отсутствие записи**: Проверка `scalar_one_or_none()` с 404 ошибкой

### 6. **WebSocket**
- ✅ **Отключение клиента**: Обработка `WebSocketDisconnect`
- ✅ **Ошибка отправки сообщения**: Удаление отключенных соединений из пула
- ✅ **Автоматическое переподключение**: Логика в `WebSocketManager` с ограничением попыток

### 7. **Frontend ошибки**
- ✅ **Ошибка парсинга WebSocket сообщения**: Try-catch с логированием
- ✅ **Ошибка загрузки wishlist**: Обработка в `catch` блоке
- ✅ **Редирект владельца**: Автоматический редирект на edit view

---

## ⚠️ Edge Cases, которые нужно доработать

### 1. **Валидация и санитизация входных данных**

#### ❌ **XSS атаки через пользовательский ввод**
- **Проблема**: Нет санитизации HTML в описаниях, названиях, bio
- **Решение**: 
  - Использовать библиотеку `DOMPurify` на frontend
  - Или экранировать HTML на backend перед сохранением

#### ❌ **SQL Injection (частично защищено ORM)**
- **Статус**: SQLAlchemy защищает от большинства случаев, но нужно проверить raw SQL
- **Решение**: Убедиться, что все запросы используют параметризованные запросы

#### ❌ **Валидация длины полей**
- **Проблема**: Нет ограничений на длину `title`, `description`, `bio`
- **Решение**: Добавить `max_length` в Pydantic схемы и валидацию на frontend

#### ❌ **Валидация формата email**
- **Проблема**: Нет проверки формата email на backend
- **Решение**: Использовать `EmailStr` из Pydantic или регулярное выражение

### 2. **Обработка ошибок сети и таймаутов**

#### ❌ **Таймауты API запросов**
- **Проблема**: Нет явных таймаутов для axios запросов
- **Решение**: Добавить `timeout` в конфигурацию axios

#### ❌ **Retry логика для failed запросов**
- **Проблема**: Нет автоматических повторов при временных сбоях
- **Решение**: Добавить retry interceptor для axios с exponential backoff

#### ❌ **Обработка медленного соединения**
- **Проблема**: Нет индикации загрузки для долгих операций
- **Решение**: Добавить loading states для всех async операций

### 3. **Обработка граничных значений**

#### ❌ **Очень большие числа (цена, сумма взноса)**
- **Проблема**: Нет проверки на максимальное значение
- **Решение**: Добавить `max_value` в Decimal валидацию

#### ❌ **Очень длинные URL**
- **Проблема**: Нет ограничения длины URL
- **Решение**: Добавить валидацию длины URL (например, 2048 символов)

#### ❌ **Пустые строки в обязательных полях**
- **Проблема**: Может пройти строка из пробелов
- **Решение**: Использовать `.strip()` и проверку на пустоту после trim

### 4. **Race conditions и транзакции**

#### ⚠️ **Race condition при удалении item во время резервирования**
- **Проблема**: Item может быть удален между проверкой и созданием резерва
- **Решение**: Использовать транзакции или проверять существование после блокировки

#### ⚠️ **Race condition при обновлении wishlist**
- **Проблема**: Два пользователя могут одновременно обновить wishlist
- **Решение**: Добавить optimistic locking или версионирование

### 5. **Обработка файлов и изображений**

#### ❌ **Валидация размера загружаемых изображений**
- **Проблема**: Нет проверки размера файла при загрузке по URL
- **Решение**: Добавить проверку Content-Length или размер после загрузки

#### ❌ **Валидация формата изображений**
- **Проблема**: Нет проверки, что URL действительно ведет на изображение
- **Решение**: Проверять Content-Type при загрузке

#### ❌ **Обработка битых ссылок на изображения**
- **Проблема**: Нет fallback для неработающих URL изображений
- **Решение**: Добавить placeholder изображение при ошибке загрузки

### 6. **WebSocket edge cases**

#### ❌ **Обработка множественных переподключений**
- **Проблема**: Может быть несколько WebSocket соединений для одного slug
- **Решение**: Проверять существующее соединение перед созданием нового

#### ❌ **Обработка переполнения памяти при большом количестве соединений**
- **Проблема**: Нет лимита на количество активных соединений
- **Решение**: Добавить максимальное количество соединений на slug

#### ❌ **Heartbeat для обнаружения мертвых соединений**
- **Проблема**: Нет механизма heartbeat для проверки активности
- **Решение**: Добавить ping/pong механизм

### 7. **Безопасность**

#### ❌ **Rate limiting**
- **Проблема**: Нет защиты от DDoS и brute force атак
- **Решение**: Добавить rate limiting middleware (например, `slowapi`)

#### ❌ **CORS для production**
- **Проблема**: Разрешенные origins могут быть слишком широкими
- **Решение**: Ограничить `ALLOWED_ORIGINS` только production доменами

#### ❌ **Валидация JWT токена**
- **Статус**: Есть проверка, но нужно убедиться в правильности алгоритма
- **Решение**: Проверить использование `HS256` и валидацию подписи

#### ❌ **Хранение токенов**
- **Проблема**: Токены в localStorage уязвимы для XSS
- **Решение**: Рассмотреть использование httpOnly cookies (требует изменений в CORS)

### 8. **Обработка ошибок пользователя**

#### ❌ **Понятные сообщения об ошибках на frontend**
- **Проблема**: Некоторые ошибки показывают технические детали
- **Решение**: Маппинг технических ошибок на понятные пользователю сообщения

#### ❌ **Валидация форм на frontend**
- **Проблема**: Нет валидации до отправки (только после ответа сервера)
- **Решение**: Добавить валидацию с помощью react-hook-form + zod

#### ❌ **Обработка частичного успеха**
- **Проблема**: Нет обработки случаев, когда операция частично выполнена
- **Решение**: Использовать транзакции и rollback при ошибках

### 9. **Производительность и масштабируемость**

#### ❌ **N+1 запросы при загрузке wishlists**
- **Проблема**: Может быть проблема с lazy loading relationships
- **Решение**: Использовать `selectinload` или `joinedload` для eager loading

#### ❌ **Отсутствие пагинации**
- **Проблема**: Все wishlists и items загружаются сразу
- **Решение**: Добавить пагинацию для списков

#### ❌ **Кэширование**
- **Проблема**: Нет кэширования часто запрашиваемых данных
- **Решение**: Добавить Redis кэш для публичных wishlists

### 10. **Другие edge cases**

#### ❌ **Обработка удаления пользователя**
- **Проблема**: Что происходит с wishlists и items при удалении аккаунта?
- **Решение**: Определить политику (cascade delete или soft delete)

#### ❌ **Обработка изменения email**
- **Проблема**: Нет эндпоинта для изменения email
- **Решение**: Добавить с подтверждением через email

#### ❌ **Валидация slug уникальности**
- **Проблема**: Может быть коллизия slug при создании
- **Решение**: Добавить проверку уникальности и генерацию нового при коллизии

#### ❌ **Обработка очень длинных описаний**
- **Проблема**: Нет ограничения на длину description
- **Решение**: Добавить `max_length` и truncate на frontend

#### ❌ **Обработка специальных символов в slug**
- **Статус**: Slug генерируется автоматически, но нужно проверить edge cases
- **Решение**: Убедиться в правильной обработке всех символов

---

## 📊 Приоритеты доработки

### 🔴 **Высокий приоритет (критично для безопасности)**
1. XSS защита (санитизация HTML)
2. Rate limiting
3. Валидация длины полей
4. Валидация формата email
5. Обработка битых ссылок на изображения

### 🟡 **Средний приоритет (важно для UX)**
1. Retry логика для failed запросов
2. Валидация форм на frontend
3. Понятные сообщения об ошибках
4. Heartbeat для WebSocket
5. Обработка множественных переподключений

### 🟢 **Низкий приоритет (улучшения)**
1. Пагинация
2. Кэширование
3. Optimistic locking
4. Изменение email
5. Валидация размера изображений

---

## 📝 Рекомендации

1. **Добавить тесты** для всех edge cases
2. **Добавить мониторинг** ошибок (например, Sentry)
3. **Добавить логирование** критических операций
4. **Создать документацию** по обработке ошибок для разработчиков
5. **Провести security audit** перед production deployment

