# 🚀 Руководство по деплою приложения

Это руководство поможет вам задеплоить приложение для демонстрации руководителю.

## 📋 Что нужно задеплоить

1. **Frontend** (React) → Vercel (бесплатно, быстро)
2. **Backend** (FastAPI) → Render или Railway (бесплатные планы)
3. **База данных** → Supabase (уже настроена)

---

## 🎯 Вариант 1: Быстрый деплой (рекомендуется для тестового задания)

### Шаг 1: Деплой Frontend на Vercel

1. **Создайте аккаунт на Vercel:**
   - Перейдите на [vercel.com](https://vercel.com)
   - Войдите через GitHub

2. **Подготовьте frontend:**
   ```bash
   cd frontend
   ```
   
   Создайте файл `vercel.json` в корне `frontend/`:
   ```json
   {
     "buildCommand": "npm run build",
     "outputDirectory": "dist",
     "devCommand": "npm run dev",
     "installCommand": "npm install",
     "framework": "vite",
     "rewrites": [
       {
         "source": "/(.*)",
         "destination": "/index.html"
       }
     ]
   }
   ```

3. **Создайте файл `.env.production` в `frontend/`:**
   ```env
   VITE_API_URL=https://your-backend-url.onrender.com
   VITE_WS_URL=wss://your-backend-url.onrender.com
   ```
   (URL бэкенда получите после деплоя backend)

4. **Закоммитьте изменения:**
   ```bash
   git add .
   git commit -m "Prepare for deployment"
   git push
   ```

5. **Деплой на Vercel:**
   - Зайдите на vercel.com
   - Нажмите "New Project"
   - Выберите ваш репозиторий
   - **Root Directory:** выберите `frontend`
   - **Framework Preset:** Vite
   - **Build Command:** `npm run build`
   - **Output Directory:** `dist`
   - **Environment Variables:** добавьте переменные из `.env.production`
   - Нажмите "Deploy"

6. **После деплоя:**
   - Vercel даст вам URL (например: `your-app.vercel.app`)
   - Обновите `VITE_API_URL` в настройках проекта Vercel

---

### Шаг 2: Деплой Backend на Render

1. **Создайте аккаунт на Render:**
   - Перейдите на [render.com](https://render.com)
   - Войдите через GitHub

2. **Создайте новый Web Service:**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий
   - Выберите репозиторий

3. **Настройки сервиса:**
   - **Name:** `social-wishlist-api` (или любое другое)
   - **Region:** выберите ближайший (например, Frankfurt)
   - **Branch:** `main` (или ваша ветка)
   - **Root Directory:** `backend`
   - **Runtime:** `Python 3`
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `uvicorn app.main:app --host 0.0.0.0 --port $PORT`

4. **Environment Variables:**
   Добавьте все переменные из `backend/.env`:
   ```
   DATABASE_URL=postgresql+asyncpg://postgres:[PASSWORD]@[HOST]:6543/postgres
   SECRET_KEY=your-secret-key-here
   ALLOWED_ORIGINS=https://your-frontend.vercel.app
   FRONTEND_URL=https://your-frontend.vercel.app
   GOOGLE_CLIENT_ID=your-google-client-id (опционально)
   GOOGLE_CLIENT_SECRET=your-google-client-secret (опционально)
   ```

5. **Нажмите "Create Web Service"**

6. **После деплоя:**
   - Render даст вам URL (например: `your-api.onrender.com`)
   - Обновите `VITE_API_URL` в Vercel настройках

---

### Шаг 3: Обновление переменных окружения

1. **В Render (Backend):**
   - Обновите `ALLOWED_ORIGINS` и `FRONTEND_URL` на URL вашего Vercel приложения

2. **В Vercel (Frontend):**
   - Обновите `VITE_API_URL` на URL вашего Render сервиса
   - Обновите `VITE_WS_URL` (замените `http` на `wss` и `ws` на `wss`)

3. **Перезапустите сервисы:**
   - В Render: нажмите "Manual Deploy" → "Deploy latest commit"
   - В Vercel: изменения применятся автоматически

---

## 🎯 Вариант 2: Деплой Backend на Railway (альтернатива)

Railway часто быстрее и проще для Python приложений.

1. **Создайте аккаунт на Railway:**
   - Перейдите на [railway.app](https://railway.app)
   - Войдите через GitHub

2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий

3. **Настройте сервис:**
   - Railway автоматически определит Python проект
   - Выберите папку `backend`
   - Railway автоматически установит зависимости

4. **Добавьте переменные окружения:**
   - Перейдите в "Variables"
   - Добавьте все переменные из `backend/.env`

5. **Настройте команду запуска:**
   - В "Settings" → "Deploy" → "Start Command":
   ```
   uvicorn app.main:app --host 0.0.0.0 --port $PORT
   ```

6. **После деплоя:**
   - Railway даст вам URL
   - Обновите переменные в Vercel

---

## 🔧 Проверка работы

1. **Откройте frontend URL:**
   - Должна открыться главная страница

2. **Проверьте API:**
   - Откройте `https://your-backend-url.onrender.com/docs`
   - Должна открыться Swagger документация

3. **Проверьте подключение:**
   - Попробуйте зарегистрироваться
   - Создайте вишлист
   - Проверьте, что данные сохраняются

---

## ⚠️ Важные моменты

### CORS
Убедитесь, что в `ALLOWED_ORIGINS` указан правильный URL frontend:
```
ALLOWED_ORIGINS=https://your-app.vercel.app
```

### WebSocket
Для WebSocket на Render/Railway используйте `wss://` вместо `ws://`:
```
VITE_WS_URL=wss://your-backend-url.onrender.com
```

### База данных
Supabase уже настроена. Убедитесь, что:
- Используете **Connection Pooling URL** (порт 6543)
- URL в формате: `postgresql+asyncpg://postgres:[PASSWORD]@[HOST]:6543/postgres`

### Google OAuth (опционально)
Если используете OAuth:
1. В Google Cloud Console добавьте redirect URI:
   - `https://your-backend-url.onrender.com/api/auth/google/callback`
2. Обновите `GOOGLE_CLIENT_ID` и `GOOGLE_CLIENT_SECRET` в Render/Railway

---

## 📝 Чеклист перед демонстрацией

- [ ] Frontend задеплоен на Vercel
- [ ] Backend задеплоен на Render/Railway
- [ ] Переменные окружения настроены
- [ ] CORS настроен правильно
- [ ] WebSocket URL использует `wss://`
- [ ] База данных подключена
- [ ] Можно зарегистрироваться
- [ ] Можно создать вишлист
- [ ] Публичная ссылка работает
- [ ] Реалтайм обновления работают

---

## 🆘 Решение проблем

### Frontend не подключается к Backend
- Проверьте `VITE_API_URL` в Vercel
- Проверьте CORS в настройках backend
- Проверьте логи в Render/Railway

### Backend не запускается
- Проверьте логи в Render/Railway
- Убедитесь, что все зависимости в `requirements.txt`
- Проверьте переменные окружения

### База данных не подключается
- Проверьте `DATABASE_URL` формат
- Убедитесь, что используете Connection Pooling URL
- Проверьте, что Supabase проект активен

---

## 🎉 Готово!

После выполнения всех шагов у вас будет:
- **Frontend:** `https://your-app.vercel.app`
- **Backend:** `https://your-api.onrender.com`
- **API Docs:** `https://your-api.onrender.com/docs`

Можете показывать руководителю! 🚀

---

## 📞 Быстрая ссылка для демонстрации

Создайте простую страницу с ссылками:
- Главная страница: [ваш-frontend-url]
- API документация: [ваш-backend-url]/docs
- Health check: [ваш-backend-url]/health

